# ┌────────────────────────────────────────────┐
# │           LOCAL DEVELOPMENT SETUP          │
# └────────────────────────────────────────────┘
# Copy this file to `.env` to get started with local development.

# ── Database Configuration ────────────────────
# Name of the database used in development
POSTGRES_DB=conversion_tracker

# Username for local PostgreSQL
POSTGRES_USER=postgres

# Password for local PostgreSQL
POSTGRES_PASSWORD=postgres

# ── External API Configuration ────────────────
# Base URL of the external sales data API
# Use host.docker.internal for accessing local services from inside Docker
EXTERNAL_API_URL=http://host.docker.internal:8081/sales-data

# The actual `.env` file is usually ignored via .gitignore
# so you can customize it freely.
